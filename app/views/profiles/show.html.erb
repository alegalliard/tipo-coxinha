<h1><%= @user.name %></h1>
<%= image_tag ('profile_avatar.jpg'), class: 'avatar' %>

<% if @user.products.empty? %>
  <h3>Desculpe, este cozinheiro ainda não cadastrou seus produtos =(</h3>
<% else %>
  <h2>Produtos</h2>
  <% @user.products.each do |product| %>
     <div class="row">
      <div class="col-md-6" id="product-<%= product.id %>">
        <%= product.name  %>
        <%= number_to_currency product.price %>
      </div>
    </div>
  <% end  %>
<% end %>
